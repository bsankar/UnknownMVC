@model UnknownMVC.Models.Task
@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>
@using (Html.BeginForm())
{
    <div>
       @Html.LabelFor(model => model.TaskDescription);
        @Html.TextAreaFor(model => model.TaskDescription);
    </div>
    <input type="submit" name="Create User" />
}